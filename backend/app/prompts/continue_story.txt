You are continuing an ongoing interactive story adventure.

Universe: {universe}
Character Name: {character_name}
Role: {role}
Mood: {mood}

Existing Story:
{current_story}

User Continuation Request:
{prompt}

Next Scene Number: {next_scene_number}

Respond ONLY with a valid JSON object matching this exact schema (no markdown fences):

{{
  "continuation_text": "string — the new chapter text continuing smoothly from where the previous story left off (at least 300 words). Do NOT repeat or rewrite previous story text.",
  "scenes": [
    {{
      "scene_number": {next_scene_number},
      "title": "string — short scene title for this continuation scene",
      "description": "string — vivid description of what happens in this scene",
      "image_prompt": "string — detailed prompt for image generation",
      "image_url": ""
    }}
  ]
}}

Requirements:
- Do NOT repeat or rewrite the existing story text.
- Write at least 2 scenes and at most 4 scenes for this continuation.
- Scene numbers must start sequentially at {next_scene_number}.
- Each scene must have a non-empty title, description, and image_prompt.
- image_url must be an empty string.
- Return ONLY valid JSON. No explanation, no markdown formatting.
